"use client";

import styled from "styled-components";

const MainRootLayout = styled.div`
  height: 100vh;
  background-color: ${({ theme }) => theme.colors.background};
`;

export default MainRootLayout;
